description: "skips child when parent fails"
steps:
  - type: exec
    command: "echo should-not-run"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "should-not-run" }
