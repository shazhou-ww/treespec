description: "judges command output with LLM"
env:
  - SILICONFLOW_API_KEY
steps:
  - type: exec
    command: "echo hello world"
    assert:
      type: llm
      prompt: "Does the output contain the word hello?"
