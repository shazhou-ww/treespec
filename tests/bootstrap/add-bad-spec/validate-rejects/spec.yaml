description: "validate should reject the broken spec"
steps:
  - type: exec
    command: "treespec validate --config /tmp/bootstrap-project/treespec.yaml 2>&1; [ $? -ne 0 ]"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "(Error|error|invalid|Invalid)" }
        - { path: "exit_code", regex: "^0$" }
