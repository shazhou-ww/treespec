description: "write state file (Phase 3 fixture)"
steps:
  - type: exec
    command: "echo test-data > /tmp/state"
    assert:
      type: regex
      conditions:
        - { path: "exit_code", regex: "^0$" }
