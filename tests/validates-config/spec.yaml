description: "validates a well-formed config and spec tree"
steps:
  - type: exec
    command: "treespec validate --config assets/validate-fixture/treespec.yaml 2>&1"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "Valid" }
        - { path: "exit_code", regex: "^0$" }
