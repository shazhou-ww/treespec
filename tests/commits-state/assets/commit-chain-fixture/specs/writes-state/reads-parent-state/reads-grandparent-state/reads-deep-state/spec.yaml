description: "reads committed state from ancestor chain"
steps:
  - type: exec
    command: "grep -q parent-data /tmp/state && grep -q child-data /tmp/state"
    assert:
      type: regex
      conditions:
        - { path: "exit_code", regex: "^0$" }
