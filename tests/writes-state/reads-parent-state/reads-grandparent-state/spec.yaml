description: "reads grandparent and parent committed state"
steps:
  - type: exec
    command: "cat /tmp/state"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "parent-data" }
        - { path: "stdout", regex: "child-data" }
        - { path: "exit_code", regex: "^0$" }
