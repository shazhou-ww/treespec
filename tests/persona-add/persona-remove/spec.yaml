description: "should be SKIPPED when persona-add fails"
steps:
  - type: exec
    command: "echo should-not-run"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "should-not-run" }
