name: treespec
image:
  dockerfile: spec/Dockerfile
  tag: treespec-fixture:base

# projectDir defaults to "." — mounted read-only at /app inside containers.
# Source, node_modules, and dist all come from the mount.
projectDir: .

spec: spec

output: .treespec-output
