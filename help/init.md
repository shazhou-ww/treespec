treespec init — create a project scaffold

Usage: treespec init <path> [--name <name>]

Options:
  --name <name>   Override test suite name (default: directory name)
  -h, --help      Show this help

Creates: treespec.yaml + spec/Dockerfile + spec/example/spec.yaml
Fails if treespec.yaml already exists.
