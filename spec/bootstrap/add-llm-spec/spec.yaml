description: "add LLM assert spec"
env:
  - TREESPEC_LLM_API_KEY
steps:
  - type: exec
    command: "cp -r /app/spec/assets/spec/llm-assert /workspace/bootstrap-project/spec/llm-assert"
    assert:
      type: regex
      conditions:
        - { path: "exit_code", regex: "^0$" }
primary: run-llm
