description: "add wait/poll spec"
steps:
  - type: exec
    command: "cp -r /app/spec/assets/spec/wait-poll /workspace/bootstrap-project/spec/wait-poll"
    assert:
      type: regex
      conditions:
        - { path: "exit_code", regex: "^0$" }
primary: run-wait
