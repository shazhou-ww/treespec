description: "verify error when branches declared without primary"
steps:
  - type: exec
    command: "cp -r /app/spec/assets/spec/no-primary-demo /workspace/bootstrap-project/spec/no-primary-demo && treespec validate --config /workspace/bootstrap-project/treespec.yaml 2>&1; [ $? -ne 0 ]"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "primary" }
        - { path: "exit_code", regex: "^0$" }
