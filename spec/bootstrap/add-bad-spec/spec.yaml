description: "add a spec with invalid assertion type"
steps:
  - type: exec
    command: "cp -r /app/spec/assets/spec/broken-spec /workspace/bootstrap-project/spec/broken-spec"
    assert:
      type: regex
      conditions:
        - { path: "exit_code", regex: "^0$" }
primary: validate-rejects
