description: "run with trace enabled, then show it"
steps:
  - type: exec
    command: "rm -rf /tmp/bootstrap-trace && treespec run --config /tmp/bootstrap-project/treespec.yaml --no-mount --rebuild --output /tmp/bootstrap-trace/trace.jsonl 2>&1; true"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "Trace" }
        - { path: "stdout", regex: "bootstrap-trace" }
        - { path: "exit_code", regex: "^0$" }
  - type: exec
    command: "test -f /tmp/bootstrap-trace/trace.jsonl && treespec show /tmp/bootstrap-trace/trace.jsonl 2>&1"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "PASS" }
        - { path: "stdout", regex: "FAIL" }
        - { path: "exit_code", regex: "^0$" }
