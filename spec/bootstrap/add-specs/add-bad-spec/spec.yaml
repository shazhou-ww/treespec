description: "add broken spec — validate and run should reject"
steps:
  - description: "copy broken spec into project"
    command: "cp -r /app/spec/assets/spec/broken-spec /workspace/greeting/spec/"
primary: validate-rejects
branches:
  - run-rejects
