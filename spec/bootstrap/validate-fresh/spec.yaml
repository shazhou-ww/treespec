description: "validate the freshly initialized project"
steps:
  - type: exec
    description: "validate should pass"
    command: "treespec validate --config /workspace/greeting/treespec.yaml 2>&1"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "Valid" }
