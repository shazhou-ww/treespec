description: "add commit chain specs — parent writes, child reads"
steps:
  - type: exec
    description: "copy write-state spec into project"
    command: "cp -r /app/spec/assets/spec/write-state /workspace/greeting/spec/write-state"
primary: run-commit-chain
