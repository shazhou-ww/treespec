description: "add commit chain specs — parent writes, child reads"
steps:
  - type: exec
    command: "cp -r /app/spec/assets/spec/write-state /workspace/bootstrap-project/spec/write-state"
    assert:
      type: regex
      conditions:
        - { path: "exit_code", regex: "^0$" }
primary: run-commit-chain
