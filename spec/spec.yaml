description: "treespec self-test tree root"
# Container node: no steps, just declares children to form a single tree.
primary: bootstrap
branches:
  - clean-tags
  - root-tree-test
  - rejects-missing-config
  - shows-help
