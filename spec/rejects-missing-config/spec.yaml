description: "treespec validate with non-existent config should fail gracefully"
steps:
  - type: exec
    description: "validate nonexistent config — should fail"
    command: "treespec validate --config /nonexistent/treespec.yaml 2>&1; [ $? -ne 0 ]"
