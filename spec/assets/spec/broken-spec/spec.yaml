description: "broken spec with invalid assert"
steps:
  - description: "run broken spec (should fail validation)"
    command: "echo hi"
    assert:
      type: nonexistent
      conditions:
        - { path: "stdout", regex: "hi" }
