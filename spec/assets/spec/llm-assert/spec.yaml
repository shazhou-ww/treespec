description: "LLM judges command output"
steps:
  - type: exec
    description: "echo text for LLM judge"
    command: "echo hello world"
    assert:
      type: llm
      prompt: "Does the output contain the word hello?"
