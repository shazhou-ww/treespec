description: "LLM judges greet output"
steps:
  - type: exec
    description: "greet with --name flag for LLM judge"
    command: "greet --name World"
    assert:
      type: llm
      prompt: "Does the output contain a greeting to someone named World?"
