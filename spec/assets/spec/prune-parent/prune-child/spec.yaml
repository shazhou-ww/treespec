description: "should be skipped when parent fails"
steps:
  - type: exec
    description: "should not run (pruned by parent failure)"
    command: "echo should-not-run"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "should-not-run" }
