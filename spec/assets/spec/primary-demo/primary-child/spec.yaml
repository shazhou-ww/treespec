description: "primary child — main line continuation"
steps:
  - type: exec
    description: "echo primary-child"
    command: "echo primary"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "primary" }
primary: leaf-a
