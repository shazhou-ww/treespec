description: "leaf under branch b"
steps:
  - type: exec
    description: "echo leaf-b"
    command: "echo leaf-b"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "leaf-b" }
