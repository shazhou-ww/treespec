description: "primary demo root"
steps:
  - type: exec
    description: "echo root"
    command: "echo root"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "root" }
primary: primary-child
branches:
  - branch-b
  - branch-c
