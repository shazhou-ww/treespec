description: "set empty name, greet fails"
steps:
  - description: "set name to empty string"
    command: "greet config set name ''"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "name=" }
  - description: "greet without name — should fail"
    command: "greet"
