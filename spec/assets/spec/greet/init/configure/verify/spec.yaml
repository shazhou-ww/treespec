description: "greet reads state — expect Hello, Alice!"
steps:
  - description: "greet reads persisted state"
    command: "greet"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "Hello, Alice" }
