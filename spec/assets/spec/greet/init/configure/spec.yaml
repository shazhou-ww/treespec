description: "greet config set name Alice — update state"
steps:
  - type: exec
    description: "set name to Alice"
    command: "greet config set name Alice"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "name=Alice" }
primary: verify
branches:
  - set-empty
  - serve
