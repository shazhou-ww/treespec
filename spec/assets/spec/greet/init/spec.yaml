description: "greet init — create state file"
steps:
  - type: exec
    description: "initialize greet state file"
    command: "greet init"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "initialized" }
        - { path: "exit_code", regex: "^0$" }
primary: configure
