description: "child reads parent greet state"
steps:
  - type: exec
    description: "greet config get should return parent's value"
    command: "greet config get name"
    assert:
      type: regex
      conditions:
        - { path: "stdout", regex: "Parent" }
