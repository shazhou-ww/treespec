description: "parent writes state"
steps:
  - type: exec
    description: "write state for child to inherit"
    command: "echo parent-data > /tmp/clean-state"
    assert:
      type: regex
      conditions:
        - { path: "exit_code", regex: "^0$" }
primary: child
